# App
NODE_ENV=production
PORT=3001
HOST=0.0.0.0
BASE_URL=https://meetshare.app
DB_PATH=/data/share-it.db
UPLOAD_DIR=/uploads
MAX_FILE_SIZE_MB=100
PURGE_DAYS=7
MAX_UPLOAD_DIR_GB=10

# Development only (not needed in production)
CLIENT_URL=http://localhost:5173

# TURN server (uses TURNS scheme for TLS via HA Proxy)
TURN_SERVER=turns:turn.meetshare.app:443
TURN_USERNAME=changeme
TURN_CREDENTIAL=changeme-use-a-strong-secret
